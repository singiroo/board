<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- 쿼리문 실행할 Sqlsession 빈 등록  -->
	<bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg ref="sqlSessionFactory"/>
	</bean>
	
	<!-- SqlSessionTemplate 객체를 생성해줄 Factory 객체 빈 등록  
		 연결 정보를 담고있는 dataSource 빈,
		 mapper 정보 및 alias 등등의 설정파일 위치 담고있는 configLocation필요-->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource"/>
		<property name="configLocation" value="classpath:kr/or/ddit/db/mybatis-config.xml"/>
	</bean>
	
	<!-- sqlSessionFactory의 설정에 필요한 dataSource 객체 빈 등록.
		 db 연결정보 필요  -->
		 
	<bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource">
		<property name="driverClassName" value="${jdbc.driver}"/>
		<property name="url" value="${jdbc.url}"/>
		<property name="username" value="${jdbc.username}"/>
		<property name="password" value="${jdbc.password}"/>
	</bean>
	
	
	<!-- db 연결 정보를 담고있는 properties 파일 연결  -->
	
	<context:property-placeholder location="classpath:kr/or/ddit/db/db_dev.properties"/>
	
</beans>
